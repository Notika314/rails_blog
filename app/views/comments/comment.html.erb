<%= comment.body %>
<%= render comment.replies if comment.replies.any? %>
<%= form_for [comment.post, comment.new] do |f| %>
   <%= f.hidden_field :post_id, comment.post.id %>
   <%= f.hidden_field :parent_id , comment.parent.id%>
   <%= f.text_field :body %>
   <%= f.submit %>
<% end %>
